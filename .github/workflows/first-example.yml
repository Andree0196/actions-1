name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome message
          run: echo "Hola mundo"

        - name: Print directory
          run: pwd

        - name: Read file
          run: cat README.md