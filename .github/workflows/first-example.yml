name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome message
          run: echo "Hola mundo"

        - name: List files
          run: ls

        - name: Read file
          run: cat README.md